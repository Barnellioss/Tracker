/*const ADD_TASK = 'ADD_TASK';
const REMOVE_TASK = 'REMOVE_TASK';


let initialState = {
    tasks: []
}

const toDoReducer = (state = initialState, action) => {
    switch (action.type) {
        case ADD_TASK:
            return {
                ...state,
                tasks: [...state.tasks, { task: action.task, id: counter }]
            }
        case REMOVE_TASK: {
            return {
                ...state,
                
            }
        }
        default:
            return state
    }
}

export const setTask = (task) => ({ type: ADD_TASK, task });
export default toDoReducer;


//let call = songsAPI.getArtistData("Starset")
//window.caller = call
*/